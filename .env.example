# FTP Server Configuration
FTP_PORT=2121
FTP_HOST=0.0.0.0

# IMPORTANT: Set this to your server's IP address for passive FTP connections
# This should be the IP address that external FTP clients can reach
# Examples: 192.168.1.100 (for local NAS), your.domain.com, or public IP
PASV_URL=192.168.1.100

# TLS / FTPS Configuration
# TLS is enabled by default using an auto-generated self-signed certificate.
# Set FTP_TLS=false to disable TLS (not recommended for public access).
# Optionally provide your own certificate and key files:
# FTP_TLS_CERT=/path/to/cert.pem
# FTP_TLS_KEY=/path/to/key.pem

# Authentication Rate Limiting
# Max failed login attempts before temporarily blocking an IP (default: 5)
# FTP_LOGIN_MAX_ATTEMPTS=5
# Time window in milliseconds for rate limiting (default: 900000 = 15 minutes)
# FTP_LOGIN_WINDOW_MS=900000

# Web Dashboard Configuration
WEB_PORT=3001
# Base URL for the web dashboard, which is used for OAuth redirects
WEB_BASE_URL=http://localhost:3001

# Data Directory Configuration
# Location where config, logs, and temporary files are stored
# In Docker: /app/data (do not change)
# In Node.js: ./data (relative to project root)
DATA_DIR=./data

# Node Environment
# Set to 'production' for production deployments
NODE_ENV=production

# Photos Directory (local filesystem destination)
# Where photos will be stored on the host machine
# In Docker: mapped via volume, internal path is /data/photos
# Set this to the directory where you want photos saved on your host
PHOTOS_DIR=./photos
